# InspectSnap

Mobile PWA for property inspection photos: pick a room, shoot in bursts, photos land in Google Drive as `Inspections/<property>/<room>/Room_001.jpg`.

Static files, no build step. Serve the folder over HTTPS (camera needs a secure context).
